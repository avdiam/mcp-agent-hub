# MCP Agent Hub — pinned dependencies (see design-decisions.md, D13)
#
# Let fastmcp resolve starlette/uvicorn/mcp transitively — do NOT pin starlette
# yourself: fastmcp 3.4.1 floors starlette>=1.0.1 (CVE-2026-48710) and a
# self-pinned older line would conflict. Run `pip freeze` after install to lock
# exact patch versions.

fastmcp>=3.4,<4          # standalone FastMCP 3.x; Streamable HTTP + http_app() mount
fastapi>=0.124,<0.125    # web framework hosting the dashboard + /api/state
uvicorn[standard]>=0.34  # ASGI server ([standard] pulls uvloop/httptools)
aiosqlite>=0.21          # async SQLite off the event loop (WAL + UPDATE...RETURNING)
jinja2>=3.1.4            # dashboard templates (>=3.1.4 clears prior CVEs)
pydantic>=2.11,<3        # transitive via fastmcp/fastapi; guard against a v3 major

# dev / test
pytest>=8
