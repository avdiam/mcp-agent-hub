# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# App runtime
hub.db
hub.db-wal
hub.db-shm

# agent-hub-live sentinel (runtime: armed while a live session is listening — AHB-5)
.claude/.agent-hub-live.active

# Local tooling / OS
.env
.DS_Store
Thumbs.db

# Antigravity (AGY) CLI workspace artifact, created when running `agy` in this dir during E2E
cache/
logs/
*.log
.gemini/
